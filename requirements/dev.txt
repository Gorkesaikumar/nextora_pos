# ---------------------------------------------------------------------------
# Development dependencies — layered ON TOP of the exact base pins.
# Never installed in production images.
# ---------------------------------------------------------------------------
-r base.txt

# Lint / format / type-check
ruff==0.4.8
mypy==1.10.0
django-stubs==5.0.2
djangorestframework-stubs==3.15.0

# Testing
pytest==8.2.2
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.0               # test data factories (no fat fixtures)
freezegun==1.5.1                 # deterministic time in tests

# Dev convenience
django-debug-toolbar==4.4.2
django-extensions==3.2.3
ipython==8.25.0

# Security / supply-chain
pip-audit==2.7.3
