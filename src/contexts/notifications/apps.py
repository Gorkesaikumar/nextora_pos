from django.apps import AppConfig

class NotificationsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "contexts.notifications"
    label = "notifications"
    verbose_name = "Enterprise Notifications"
